# AI 爆款文章创作器 — 系统设计文档

> **项目定位**: 基于多智能体（Multi-Agent）架构的 AI 文章创作平台，用户输入选题，AI 自动完成标题生成、大纲规划、正文撰写、配图匹配和图文合成全流程。采用**人机协同交互**模式，每个关键节点让用户参与决策，确保产出符合预期。

---

## 目录

1. [系统概述](#1-系统概述)
2. [整体架构](#2-整体架构)
3. [多智能体设计（核心亮点）](#3-多智能体设计核心亮点)
4. [三阶段交互流程](#4-三阶段交互流程)
5. [异步 + SSE 实时推送](#5-异步--sse-实时推送)
6. [关键设计决策](#6-关键设计决策)
7. [技术栈](#7-技术栈)
8. [数据库设计](#8-数据库设计)
9. [面试 Q&A 速查](#9-面试-qa-速查)

---

## 1. 系统概述

### 1.1 要解决什么问题

传统 AI 写文章产品存在三个核心痛点：

| 痛点 | 表现 | 本方案解决方式 |
|------|------|---------------|
| **一次性生成不可控** | 用户给个选题，AI 生成全文，结果不符合预期 | 三阶段拆分，每阶段用户确认后再继续 |
| **标题/大纲质量差** | AI 直接写正文，标题和结构没人把关 | 专用 Agent 生成标题方案和大纲，人工精选 |
| **图文分离** | 只生成文字，配图需单独处理 | 内置配图需求分析 + 多通道图片生成 + 自动合成 |

### 1.2 核心使用流程

```
用户输入选题 → AI 生成标题方案（3-5套）
             → 用户选择一套标题 + 补充描述
             → AI 生成结构化大纲（流式展示）
             → 用户确认大纲（或 AI 辅助修改）
             → AI 生成正文 + 分析配图需求 + 并行生成配图 + 图文合成
             → 输出完整图文文章
```

---

## 2. 整体架构

### 2.1 分层架构图

```
┌──────────────────────────────────────────────────────────────────────────┐
│                             前端 (Web)                                   │
│   创建任务    选择标题    确认大纲    SSE实时展示   文章管理              │
└──────────────────────────┬───────────────────────────────────────────────┘
                           │ REST API + SSE
                           ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                        Controller 层                                    │
│  ArticleController    UserController    PaymentController               │
│  HealthController     StatisticsController                               │
│  StripeWebhookController                                                 │
├──────────────────────────────────────────────────────────────────────────┤
│                        异步服务层                                        │
│  ArticleAsyncService (@Async)  ────  SseEmitterManager (SSE推送)        │
│   ├── exectePhase1() 生成标题                                            │
│   ├── exectePhase2() 生成大纲                                            │
│   └── exectePhase3() 生成正文+配图                                       │
├──────────────────────────────────────────────────────────────────────────┤
│                     多智能体编排层                                        │
│  ┌────────────────────────────────────────────┐                         │
│  │ ArticleAgentOrchestrator (StateGraph)      │                         │
│  │  ├── buildPhase1Graph() → [TitleGenerator] │                         │
│  │  ├── buildPhase2Graph() → [OutlineGenerator]│                         │
│  │  └── buildPhase3Graph() → [ContentGen] →   │                         │
│  │                         [ImageAnalyzer] →   │                         │
│  │                   [ParallelImageGen] →       │                         │
│  │                         [ContentMerger]      │                         │
│  └────────────────────────────────────────────┘                         │
│  或                                                                      │
│  ┌────────────────────────────────────────────┐                         │
│  │ ArticleAgentService (手动编排·旧模式)       │                         │
│  └────────────────────────────────────────────┘                         │
├──────────────────────────────────────────────────────────────────────────┤
│                       业务 Service 层                                    │
│  ArticleService    UserService    ImageServiceStrategy                   │
│  CosService        PaymentService  AgentLogService                       │
├──────────────────────────────────────────────────────────────────────────┤
│                         数据层                                            │
│  MySQL (MyBatis-Flex ORM)    Redis (会话缓存)   腾讯云 COS (图片)        │
└──────────────────────────────────────────────────────────────────────────┘
```

### 2.2 核心包结构

```
com.ysw.aipassagecreator
├── agent/                     # 多智能体核心
│   ├── ArticleAgentOrchestrator.java   # StateGraph 编排器
│   ├── config/AgentConfig.java         # 智能体配置
│   ├── agents/                         # 5 大 Agent 实现
│   │   ├── TitleGeneratorAgent.java
│   │   ├── OutlineGeneratorAgent.java
│   │   ├── ContentGeneratorAgent.java
│   │   ├── ImageAnalyzerAgent.java
│   │   └── ContentMergerAgent.java
│   ├── parallel/ParallelImageGenerator.java  # 并行配图生成
│   ├── tools/ImageGenerationTool.java        # 配图生成工具
│   └── context/StreamHandlerContext.java     # 流式上下文(ThreadLocal)
├── controller/                # REST + SSE 接口
├── service/                   # 业务逻辑
├── manager/SseEmitterManager.java  # SSE 连接管理
├── config/                    # 全局配置（线程池、CORS、COS、Stripe等）
├── aop/                       # Agent 执行日志切面 + 权限拦截
├── constant/PromptConstant.java  # 所有 AI Prompt 模板
├── mapper/                    # MyBatis-Flex 数据访问
├── model/                     # 实体、DTO、VO、枚举
└── exception/                 # 全局异常处理
```

---

## 3. 多智能体设计（核心亮点）

### 3.1 五大智能体分工

| # | 智能体 | 类名 | 职责 | 输入 | 输出 | 是否流式 |
|---|--------|------|------|------|------|---------|
| 1 | **标题生成器** | `TitleGeneratorAgent` | 根据选题生成 3-5 套标题方案 | 选题 + 风格 | `List<TitleOption>` | 否 |
| 2 | **大纲生成器** | `OutlineGeneratorAgent` | 生成结构化文章大纲（章节+要点） | 标题 + 补充描述 | `OutlineResult` | **是** |
| 3 | **正文生成器** | `ContentGeneratorAgent` | 按大纲扩写成完整正文 | 大纲 + 风格 | 正文 Markdown | **是** |
| 4 | **配图分析器** | `ImageAnalyzerAgent` | 分析正文中哪些位置需要配图 | 正文 | 带占位符正文 + 配图需求列表 | 否 |
| 5 | **图文合成器** | `ContentMergerAgent` | 将配图 URL 替换到正文占位符位置 | 正文 + 图片列表 | 完整图文 Markdown | 否 |

**额外组件**:
- **`ParallelImageGenerator`** — 非 LLM 节点，并行调用 Pexels 图库 / AI 生图，多张图同时生成
- **`ImageGenerationTool`** — 底层配图工具，封装 Pexels API 和 Nano Banana AI 生图两种来源

### 3.2 编排方式：StateGraph 图编排

使用 **Spring AI Alibaba StateGraph**（类似 LangGraph）将 Agent 组织为有向无环图（DAG）。

#### 阶段 1 图 — 单节点

```
[START] ──→ [TitleGeneratorAgent] ──→ [END]
```

#### 阶段 2 图 — 单节点（流式）

```
[START] ──→ [OutlineGeneratorAgent] ──→ [END]
```

#### 阶段 3 图 — 4 节点顺序链

```
[START] ──→ [ContentGeneratorAgent]
              ──→ [ImageAnalyzerAgent]
                ──→ [ParallelImageGenerator]
                  ──→ [ContentMergerAgent] ──→ [END]
```

#### 核心代码示意

```java
// 构建阶段3图：顺序执行4个节点
private StateGraph buildPhase3Graph() throws GraphStateException {
    return new StateGraph(createKeyStrategyFactory())
        .addNode("content_generator",     node_async(contentGeneratorAgent))
        .addNode("image_analyzer",        node_async(imageAnalyzerAgent))
        .addNode("parallel_image_generator", node_async(parallelImageGenerator))
        .addNode("content_merger",        node_async(contentMergerAgent))
        .addEdge(START, "content_generator")
        .addEdge("content_generator",     "image_analyzer")
        .addEdge("image_analyzer",        "parallel_image_generator")
        .addEdge("parallel_image_generator", "content_merger")
        .addEdge("content_merger",        END);
}
```

**状态管理**: 每个键使用 `ReplaceStrategy`，节点之间通过 `OverAllState` 共享数据：

```
taskId → topic → titleOptions → outline → content → imageRequirements → images → fullContent
```

### 3.3 Agent 实现模式（NodeAction 接口）

每个 Agent 实现 `NodeAction` 接口的 `apply(OverAllState)` 方法：

```java
@Component
@RequiredArgsConstructor
public class TitleGeneratorAgent implements NodeAction {
    private final DashScopeChatModel dashScopeChatModel;

    @Override
    public Map<String, Object> apply(OverAllState state) throws Exception {
        // 1. 从全局状态取值
        String topic = state.value("topic")
            .map(Object::toString)
            .orElseThrow(() -> new IllegalArgumentException("缺少选题"));

        // 2. 构造 Prompt 并调用大模型
        ChatResponse response = chatModel.call(
            new Prompt(new UserMessage(buildPrompt(topic))));

        // 3. 解析结果为结构化数据
        List<TitleOption> options = parseResult(response.getResult().getOutput().getText());

        // 4. 写回全局状态
        return Map.of("titleOptions", options);
    }
}
```

**设计要点**:
- 构造器注入 `DashScopeChatModel`（通义千问）
- 每个 Agent 只从 `OverAllState` 读自己需要的键，只写自己对应的输出键
- Prompt 模板集中管理在 `PromptConstant.java`，支持 4 种文章风格（科技/情感/教育/幽默）

### 3.4 流式输出的特殊处理

**问题**: StateGraph 的 `NodeAction.apply()` 返回 Map，没有天然流式通道，但大纲和正文生成需要逐字推送到前端。

**方案**: 使用 `ThreadLocal<Consumer<String>>` 传递流式回调

```java
// 编排器中：执行前设置 ThreadLocal，执行后清理
StreamHandlerContext.set(streamHandler);
try {
    compiledGraph.invoke(inputs);
} finally {
    StreamHandlerContext.clear();
}

// Agent 内部：从 ThreadLocal 获取 Consumer
Consumer<String> handler = StreamHandlerContext.get();
// 每当有流式输出时，调用 handler.accept(chunk)
```

**为什么选 ThreadLocal 而非参数传递?**
- `OverAllState` 中的值需可序列化，`Consumer` 不是
- 生命周期是单个异步线程，不会跨线程泄漏
- `finally` 块确保一定清理，避免内存泄漏

### 3.5 双模式切换

通过配置开关支持两套智能体引擎：

```yaml
article:
  agent:
    orchestrator:
      enabled: true   # true = StateGraph编排; false = 手动编排
    max-iterations: 10
```

- **新模式（StateGraph）**: 节点化、流程可视化、易扩展
- **旧模式（ArticleAgentService）**: 直白的顺序调用，便于调试和兜底

---

## 4. 三阶段交互流程

### 4.1 流程图

```
用户                    前端                  后端                   AI
 │                       │                    │                     │
 ├── 输入选题+风格 ──────► POST /article/create                     │
 │                       │                    │                     │
 │                       │                    ├── 创建任务(taskId)  │
 │                       │                    ├── 扣减用户配额      │
 │                       │                    └── @Async exectePhase1()
 │                       │                        │                │
 │                       │ ◄─── SSE: 进度推送 ────┤                │
 │                       │                        ├── TitleGeneratorAgent
 │                       │                        │    (生成3-5个方案)
 │                       │ ◄─── SSE: TITLES_GENERATED              │
 │                       │                        │                │
 ├── 浏览标题方案 ◄──────┤                        │                │
 │                       │                        │                │
 ├── 选择标题+补充描述 ──► POST /article/confirm-title              │
 │                       │                        │                │
 │                       │                    ├── 保存标题到DB     │
 │                       │                    └── @Async exectePhase2()
 │                       │                        │                │
 │                       │ ◄─── SSE: 流式大纲 ────┤                │
 │                       │                        ├── OutlineGeneratorAgent
 │                       │                        │    (流式输出章节+要点)
 │                       │ ◄─── SSE: OUTLINE_GENERATED             │
 │                       │                        │                │
 ├── 浏览大纲 ◄──────────┤                        │                │
 │  (可选 AI 修改)       │                        │                │
 │                       │                        │                │
 ├── 确认大纲 ───────────► POST /article/confirm-outline            │
 │                       │                        │                │
 │                       │                    ├── 保存大纲到DB     │
 │                       │                    └── @Async exectePhase3()
 │                       │                        │                │
 │                       │ ◄─── SSE: 进度推送 ────┤                │
 │                       │                        ├── ContentGeneratorAgent
 │                       │                        │    (流式正文)
 │                       │                        ├── ImageAnalyzerAgent
 │                       │                        │    (分析配图需求)
 │                       │                        ├── ParallelImageGenerator
 │                       │                        │    (并行生成多张图)
 │                       │                        └── ContentMergerAgent
 │                       │                             (占位符替换)
 │                       │ ◄─── SSE: ALL_COMPLETE                 │
 │                       │                        │                │
 ├── 查看文章 ◄────────── GET /article/{taskId}   │                │
```

### 4.2 三个阶段的详细说明

#### Phase 1 — 标题生成

| 项目 | 内容 |
|------|------|
| 触发时机 | 用户提交选题后自动触发 |
| 异步方式 | `@Async("articleExecutor")` |
| AI 工作 | 调用通义千问生成 3-5 套 `(主标题, 副标题)` |
| 状态更新 | `PROCESSING` → 生成完成 → 保存到 DB |
| SSE 事件 | `AGENT1_COMPLETE` → `TITLES_GENERATED`（含完整列表） |
| 用户操作 | 选择一套标题，可补充描述信息 |

#### Phase 2 — 大纲生成

| 项目 | 内容 |
|------|------|
| 触发时机 | 用户确认标题后自动触发 |
| 异步方式 | `@Async("articleExecutor")` |
| AI 工作 | 流式生成结构化大纲（多个章节，每章节含标题+要点列表） |
| 流式体验 | 每生成一段内容立即通过 SSE 推送到前端 |
| 用户操作 | 确认大纲，或输入修改建议让 AI 调整 |

#### Phase 3 — 正文 + 配图生成

| 项目 | 内容 |
|------|------|
| 触发时机 | 用户确认大纲后自动触发 |
| 异步方式 | `@Async`（使用默认线程池） |
| 内部流程 | **4 个 Agent 顺序执行**，构成完整流水线 |
| 配图策略 | 支持 Pexels 图库检索 + AI 生图，用户可配置 |
| 最终产出 | 完整的图文 Markdown 内容，图片通过占位符替换插入 |

### 4.3 SSE 消息类型

```java
AGENT1_COMPLETE    // 标题Agent完成
AGENT2_STREAMING   // 大纲流式输出（带前缀标识）
AGENT2_COMPLETE    // 大纲Agent完成
AGENT3_STREAMING   // 正文流式输出（带前缀标识）
AGENT3_COMPLETE    // 正文Agent完成
AGENT4_COMPLETE    // 配图需求分析完成
IMAGE_COMPLETE     // 单张配图生成完成（带图片URL）
AGENT5_COMPLETE    // 配图生成全部完成
MERGE_COMPLETE     // 图文合成完成
TITLES_GENERATED   // 阶段1完成（含标题选项列表）
OUTLINE_GENERATED  // 阶段2完成（含大纲结构）
ALL_COMPLETE       // 全部完成
ERROR              // 错误信息
```

---

## 5. 异步 + SSE 实时推送

### 5.1 异步线程池配置

```java
@Bean(name = "articleExecutor")
public Executor articleExecutor() {
    ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
    executor.setCorePoolSize(5);              // 核心线程
    executor.setMaxPoolSize(10);              // 最大线程
    executor.setQueueCapacity(100);           // 队列容量
    executor.setThreadNamePrefix("article-async-");
    executor.setRejectedExecutionHandler(
        new ThreadPoolExecutor.CallerRunsPolicy());  // 拒绝策略：调用方自己跑
    executor.setAwaitTerminationSeconds(60);
    return executor;
}
```

**拒绝策略选择 `CallerRunsPolicy` 的原因**:
- 当线程池满时，由调用方线程（Tomcat 线程）同步执行
- 相当于降级为同步生成，虽然慢但不会丢任务
- 相比 `AbortPolicy` 更友好（不抛异常），相比 `DiscardPolicy` 不丢数据

### 5.2 SSE 连接管理

```java
public class SseEmitterManager {
    // 核心数据结构：ConcurrentHashMap 保证线程安全
    private final Map<String, SseEmitter> emitterMap = new ConcurrentHashMap<>();

    public SseEmitter createSseEmitter(String taskId) {
        SseEmitter emitter = new SseEmitter(SSE_TIMEOUT_MS);
        emitter.onTimeout(() -> emitterMap.remove(taskId));    // 超时清理
        emitter.onCompletion(() -> emitterMap.remove(taskId));  // 完成清理
        emitter.onError(e -> emitterMap.remove(taskId));        // 错误清理
        emitterMap.put(taskId, emitter);
        return emitter;
    }

    public void send(String taskId, String messages) {
        SseEmitter emitter = emitterMap.get(taskId);
        if (emitter == null) return;
        emitter.send(SseEmitter.event().data(messages).reconnectTime(SSE_RECONNECT_TIME_MS));
    }
}
```

**设计要点**:
- `ConcurrentHashMap` 支持多线程并发读写
- 三种回调（timeout/completion/error）确保连接异常时自动清理
- 每次 `send()` 都检查 emitter 是否存在，防止空指针
- 前端可通过 `reconnectTime` 自动重连

### 5.3 消息路由机制

`ArticleAsyncService` 中的 `handlerAgentMessage` 负责消息分发：

```
Agent 原始消息
    │
    ├── 带 PREFIX2 前缀 → AGENT2_STREAMING（大纲流式片段）
    ├── 带 PREFIX3 前缀 → AGENT3_STREAMING（正文流式片段）
    ├── 带 IMAGE 前缀   → IMAGE_COMPLETE（图片URL）
    └── 枚举值           → AGENT*_COMPLETE / TITLES_GENERATED / OUTLINE_GENERATED …
                           （阶段完成通知，携带对应数据）
```

---

## 6. 关键设计决策

### 决策 1: 为什么用 StateGraph 而非手动编排？

| 维度 | 手动编排 | StateGraph 编排 |
|------|---------|----------------|
| 代码量 | 随 Agent 数线性增长 | 加一个节点 = 一行 addNode + 一行 addEdge |
| 流程可见性 | 藏在代码逻辑中 | Graph 结构清晰可见 |
| 扩展性 | 加逻辑需改已有方法 | 加节点不影响现有节点 |
| 状态管理 | 手动传递 Map | OverAllState + KeyStrategy 自动管理 |
| 调试 | 日志逐行分析 | 可断点查看图中每个节点的输入/输出 |

**结论**: 当 Agent 数量超过 2 个时，StateGraph 的维护成本远低于手动编排。

### 决策 2: 为什么拆成三阶段而非一次性生成？

1. **降低风险**: 标题不对 → 停止，不浪费后续 token 开销
2. **提升质量**: 用户在每个阶段补充信息（如 Phase2 的补充描述），让 AI 更准确理解意图
3. **用户体验**: 大段等待 AI 生成全部内容会让用户焦虑，阶段性反馈让用户感觉"在掌控中"
4. **业务灵活性**: 可独立优化每个阶段的 Prompt，不影响其他阶段

### 决策 3: 为什么 SSE 而非 WebSocket？

| 对比项 | SSE | WebSocket |
|--------|-----|-----------|
| 通信方向 | 服务端→客户端（单向） | 全双工 |
| 协议 | 基于 HTTP | 独立协议（ws://） |
| 浏览器兼容 | 除 IE 外全支持 | 全支持 |
| 自动重连 | 原生支持 | 需自己实现 |
| 适用场景 | 服务端推送 | 双向实时通信（如聊天） |

**结论**: 文章生成是**纯服务端推送**场景，SSE 更轻量、更简单，自带重连机制。

### 决策 4: 为什么 ThreadLocal 而非参数传递流式 Consumer？

- `OverAllState` 要求值可序列化，`Consumer<String>` 不是
- ThreadLocal 生命周期绑定单个异步线程，不会跨线程污染
- finally 块确保清理，不泄漏

### 决策 5: 双通道配图策略

```
用户配置 enabledImageMethods:
    ├── ["PEXELS"]           → 只从 Pexels 图库检索
    ├── ["NANO_BANANA"]      → 只用 AI 生图
    ├── ["PEXELS","NANO_BANANA"] → 两种都用，按权重择优
    └── null                  → 默认全部启用
```

- **Pexels 图库**: 速度快、零成本、真实图片
- **Nano Banana AI 生图**: 定制化强、与文章内容更贴合，但需要成本

### 决策 6: 配额控制

在创建文章的**同一事务**中扣减用户配额，防止并发超卖：

```java
@Transactional
public String createArticleTaskWithQuotaCheck(topic, style, loginUser) {
    // 1. 检查用户余额
    // 2. 扣减配额
    // 3. 创建文章任务
    // 4. 返回 taskId
}
```

---

## 7. 技术栈

### 7.1 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 3.5.9 | 基础框架 |
| Java | 21 | 运行环境 |
| Maven | - | 构建工具 |
| MyBatis-Flex | 1.11.1 | ORM 框架 |
| Redis + Redisson | 3.50.0 | 会话缓存 |
| Spring AI Alibaba DashScope | 1.1.0.0-RC2 | 通义千问大模型接入 + StateGraph 编排 |
| Knife4j | - | API 文档 |
| HikariCP | - | 数据库连接池 |
| Hutool | - | 工具类库 |
| Lombok | - | 代码简化 |
| Gson | - | JSON 序列化 |

### 7.2 外部服务

| 服务 | 用途 |
|------|------|
| 通义千问 (DashScope) | AI 大模型，所有 Agent 的推理引擎 |
| 腾讯云 COS | 图片存储（配图、用户头像等） |
| Pexels API | 免费图库检索配图 |
| Nano Banana API | AI 生图服务 |
| Stripe | 支付集成 |

### 7.3 中间件

| 组件 | 用途 |
|------|------|
| MySQL | 业务数据库（文章、用户、支付记录） |
| Redis | Session 存储（30天过期） |

---

## 8. 数据库设计

### 8.1 核心表

```
┌──────────────────┐   ┌──────────────────┐   ┌──────────────────┐
│      User        │   │     Article      │   │   AgentLog       │
├──────────────────┤   ├──────────────────┤   ├──────────────────┤
│ id               │   │ id               │   │ id               │
│ username         │   │ taskId (唯一)     │   │ taskId           │
│ email            │   │ userId           │   │ agentName        │
│ password         │   │ topic            │   │ status           │
│ quota            │◄──│ style            │──►│ input            │
│ ...              │   │ mainTitle        │   │ output           │
└──────────────────┘   │ subTitle         │   │ durationMs       │
                       │ userDescription  │   │ errorMessage     │
┌──────────────────┐   │ outline (JSON)   │   │ createTime       │
│  PaymentRecord   │   │ content          │   └──────────────────┘
├──────────────────┤   │ fullContent      │
│ id               │   │ status           │
│ userId           │   │ phase            │
│ amount           │   │ ...              │
│ status           │   └──────────────────┘
│ createTime       │
└──────────────────┘
```

### 8.2 文章状态机

```
PENDING ──→ PROCESSING ──→ COMPLETED
                │
                └──→ FAILED
```

阶段字段（phase）独立于状态，描述当前进度：

```
TITLE_GENERATING → TITLE_SELECTED → OUTLINE_GENERATING
→ OUTLINE_EDITING → CONTENT_GENERATING → CONTENT_COMPLETED
```

---

## 9. 面试 Q&A 速查

### Q1: 为什么需要 5 个 Agent？
**答**: 不是拍脑袋分的，是从**职责单一原则**出发的设计：
- **标题生成**和**大纲生成**分开，因为用户需要中间确认，不能合并
- **配图分析**和**正文生成**分开，因为配图分析需要通读全文得出每段配什么图，逻辑不同
- **图文合成**独立是为了支持多种配图来源（先并行生成完图片，再统一替换）

### Q2: 为什么不用 LangChain/LangGraph 等成熟框架？
**答**: 项目技术栈是 Spring Boot + Java，没有使用 Python 生态。Spring AI Alibaba 提供的 StateGraph 和 LangGraph 概念对齐（Node/Edge/State），学习成本低，且与阿里云 DashScope 原生集成，无需额外适配。

### Q3: 如何处理 AI 接口超时/失败？
**答**: 三层保护：
1. **异步线程**隔离，失败不影响主请求
2. **状态更新**为 FAILED 并保存错误信息
3. **SSE 推送** ERROR 事件通知前端
同时 `@AgentExecution` 切面记录了每个 Agent 的执行日志，方便排查。

### Q4: 如何保证 SSE 连接可靠性？
**答**: 
- 服务端：三种回调（timeout/completion/error）自动清理
- 前端：SSE 原生 `reconnectTime` 自动重连
- 设计上：任务状态持久化在数据库，即使刷新页面也能通过 `taskId` 恢复

### Q5: 并发场景下配额扣减怎么保证不超？
**答**: 在 `@Transactional` 事务中完成"检查余额 → 扣减 → 创建任务"三步操作，利用数据库行锁保证原子性。没有用乐观锁因为配额扣减冲突概率低，事务开销可接受。

---

> **文档版本**: v1.0 | **最后更新**: 2026-06-17
> **关联项目**: [ai-passage-creator](E:/ai-passage-creator/ai-passage-creator/)
